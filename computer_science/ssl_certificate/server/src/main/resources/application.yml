server:
  port: 8443
  ssl:
    enabled: true
    key-store: file:/certs/server.p12
    key-store-password: changeit
    key-store-type: PKCS12
    key-alias: server
    client-auth: none

logging:
  level:
    root: INFO
    org.springframework.web: DEBUG
